import * as ServerCore from '@var3n1k/server-core';
import * as TelegramAPI from '../../__api.js';
import * as Dependencies from '../../__dependencies.js';
export default class BaseEvent {
    // TODO: JSDoc
    _API;
    // TODO: JSDoc
    constructor(api, executable) {
        const Validator = ServerCore.Engine.Module.Classes.Validator;
        Validator.Strict(api, new Validator().Default.Class.Instance().Required().Of(TelegramAPI.default));
        Validator.Strict(executable, new Validator().Default.Function().Required());
        this._API = api;
        const MultipleEventListener = ((..._) => this._API.Client.on(..._));
        executable(Dependencies.TelegrafFilters, MultipleEventListener);
    }
}
//# sourceMappingURL=BaseEvent.js.map