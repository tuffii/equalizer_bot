import * as GlobalModule from '../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default async function Sleep(ms) {
    const Validator = GlobalModule.Classes.Validator;
    Validator.Strict(ms, new Validator().Default.Numeric().Number().Required().Integer().Min(1));
    const AwaitTimeout = (ms) => {
        return new Promise(async (resolve, reject) => {
            GlobalModule.Dependencies.Default.Node.timers.setTimeout(() => {
                resolve(undefined);
            }, ms);
        });
    };
    await AwaitTimeout(ms);
}
//# sourceMappingURL=Sleep.js.map