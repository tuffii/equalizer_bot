import * as GlobalModule from '../../../../../../@module.js';
const IsStringANewValidURL = (string) => {
    let IsStringURLBased = false;
    try {
        GlobalModule.Dependencies.Default.Node.url.parse(string);
        IsStringURLBased = true;
    }
    catch (e) {
        IsStringURLBased = false;
    }
    return IsStringURLBased;
};
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function IsURL(parameter) {
    let InstanceMatch = false;
    if (GlobalModule.Functions.Parameter.Type.Guard.Default.IsString(parameter)) {
        if (!GlobalModule.Functions.Parameter.Type.Guard.Default.IsEmptyString(parameter)) {
            if (IsStringANewValidURL(parameter))
                InstanceMatch = true;
        }
    }
    return InstanceMatch;
}
//# sourceMappingURL=IsURL.js.map