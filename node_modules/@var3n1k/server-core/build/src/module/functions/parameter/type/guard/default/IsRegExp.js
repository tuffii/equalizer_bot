import * as GlobalModule from '../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function IsRegExp(parameter) {
    let InstanceMatch = false;
    if (GlobalModule.Functions.Parameter.Type.Guard.Default.IsInstanceOf(parameter, RegExp))
        InstanceMatch = true;
    return InstanceMatch;
}
//# sourceMappingURL=IsRegExp.js.map