import * as GlobalModule from '../../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function Shorten(initialString, maxLength) {
    const Validator = GlobalModule.Classes.Validator;
    Validator.Strict(initialString, new Validator().Default.String().Required());
    Validator.Strict(maxLength, new Validator().Default.Numeric().Number().Required().Integer().Min(1));
    const ShortenPrefix = ``;
    const ShortenPostfix = `...`;
    const MaximumShortenStringLength = maxLength - (ShortenPrefix.length + ShortenPostfix.length);
    let ShortenString = initialString;
    if (ShortenString.length > maxLength) {
        ShortenString = `${ShortenPrefix}${initialString.slice(0, MaximumShortenStringLength)}${ShortenPostfix}`;
    }
    return ShortenString;
}
//# sourceMappingURL=Shorten.js.map