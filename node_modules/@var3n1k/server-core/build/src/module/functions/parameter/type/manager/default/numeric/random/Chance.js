import * as GlobalModule from '../../../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function Chance(chance) {
    const Validator = GlobalModule.Classes.Validator;
    Validator.Strict(chance, new Validator().Default.Numeric().Number().Required().Min(0).Max(1));
    const RandomNumber = GlobalModule.Functions.Parameter.Type.Manager.Default.Numeric.Random.InRange(0, 1);
    const IsChanceValid = RandomNumber <= chance;
    return IsChanceValid;
}
//# sourceMappingURL=Chance.js.map