import * as GlobalModule from '../../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function ToPercent(initialNumber) {
    const Validator = GlobalModule.Classes.Validator;
    Validator.Strict(initialNumber, new Validator().Default.Numeric().Number().Required().Min(0));
    const NumberInPercent = initialNumber * 100;
    const FixedPercent = GlobalModule.Functions.Parameter.Type.Manager.Default.Numeric.ToFixed(NumberInPercent, `${Math.floor(NumberInPercent)}`.length, 2, GlobalModule.DataSet.Dictionary.Symbol.Single.Service.Separator.Space.Default);
    const PercentifiedNumber = `${FixedPercent}%`;
    return PercentifiedNumber;
}
//# sourceMappingURL=ToPercent.js.map