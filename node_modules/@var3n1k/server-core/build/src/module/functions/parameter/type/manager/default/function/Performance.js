import * as GlobalModule from '../../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default async function Performance(initialFunction) {
    const Validator = GlobalModule.Classes.Validator;
    Validator.Strict(initialFunction, new Validator().Default.Function().Required());
    const PerformanceStart = GlobalModule.Classes.Process.Usage.Uptime.HighResolution;
    await initialFunction();
    const PerformanceEnd = GlobalModule.Classes.Process.Usage.Uptime.HighResolution;
    const PerformanceRange = (PerformanceEnd - PerformanceStart) / Math.pow(10, 9); // ns | µs | ms | s
    return PerformanceRange;
}
//# sourceMappingURL=Performance.js.map