import * as GlobalModule from '../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function IsUnknown(parameter) {
    let InstanceMatch = false;
    if (GlobalModule.Functions.Parameter.Type.Guard.Default.IsNullOrUndefined(parameter))
        InstanceMatch = true;
    if (GlobalModule.Functions.Parameter.Type.Guard.Default.IsNaN(parameter))
        InstanceMatch = true;
    if (GlobalModule.Functions.Parameter.Type.Guard.Default.IsInfinity(parameter))
        InstanceMatch = true;
    return InstanceMatch;
}
//# sourceMappingURL=IsUnknown.js.map