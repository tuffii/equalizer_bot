import * as GlobalModule from '../../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function Clear(initialArray) {
    const Validator = GlobalModule.Classes.Validator;
    Validator.Strict(initialArray, new Validator().Default.Array().Required());
    while (!Validator.TypeGuard.Default.IsEmptyArray(initialArray)) {
        initialArray.shift();
    }
}
//# sourceMappingURL=Clear.js.map