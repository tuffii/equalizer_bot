import * as GlobalModule from '../../../../../../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function ToPascalCase(initialString) {
    const Validator = GlobalModule.Classes.Validator;
    Validator.Strict(initialString, new Validator().Default.String().Required());
    const InitialStringWords = GlobalModule.Functions.Parameter.Type.Manager.Default.String.MultiSplit(initialString, /[_]+/);
    const InitialStringWordsToLowerCase = InitialStringWords.map((_el, _ind, _arr) => {
        return GlobalModule.Functions.Parameter.Type.Manager.Default.String.Style.Modify.Case.Size.ToLowerCase(_el, Array.from(Array(_el.length), (__el, __ind) => __ind));
    });
    const PascalCaseWordsToUpperCase = InitialStringWordsToLowerCase.map((_el, _ind, _arr) => {
        return GlobalModule.Functions.Parameter.Type.Manager.Default.String.Style.Modify.Case.Size.ToUpperCase(_el, [0]);
    });
    const ModifiedString = PascalCaseWordsToUpperCase.join(``);
    return ModifiedString;
}
//# sourceMappingURL=ToPascalCase.js.map