import * as GlobalModule from '../../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function RGBToHEX(...rgbColor) {
    const Validator = GlobalModule.Classes.Validator;
    Validator.Strict(rgbColor, new Validator().Custom.Color.RGB().Required());
    const HEX_Red = rgbColor[0] << 16;
    const HEX_Green = rgbColor[1] << 8;
    const HEX_Blue = rgbColor[2];
    const HEXColorValueToInt = HEX_Red + HEX_Green + HEX_Blue;
    const HEXColorValueToString = `${HEXColorValueToInt.toString(16)}`.padStart(6, `${0}`);
    const HEXColor = `#${HEXColorValueToString}`;
    const ToUpperCase = GlobalModule.Functions.Parameter.Type.Manager.Default.String.Style.Modify.Case.Size.ToUpperCase;
    const HEXColorUpperCased = ToUpperCase(HEXColor, Array.from(Array(HEXColor.length), (_el, _ind) => _ind));
    return HEXColorUpperCased;
}
//# sourceMappingURL=RGBToHEX.js.map