import * as GlobalModule from '../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function IsObject(parameter) {
    let InstanceMatch = false;
    if (!GlobalModule.Functions.Parameter.Type.Guard.Default.IsUnknown(parameter)) {
        // if (GlobalModule.Functions.Parameter.Type.Guard.Default.IsInstanceOf(parameter, Object))
        if (typeof parameter === 'object') {
            if (!GlobalModule.Functions.Parameter.Type.Guard.Default.IsArray(parameter)) {
                if (!GlobalModule.Functions.Parameter.Type.Guard.Default.IsRegExp(parameter)) {
                    if (!GlobalModule.Functions.Parameter.Type.Guard.Default.IsDate(parameter)) {
                        if (!GlobalModule.Functions.Parameter.Type.Guard.Default.IsError(parameter))
                            InstanceMatch = true;
                    }
                }
            }
        }
    }
    return InstanceMatch;
}
//# sourceMappingURL=IsObject.js.map