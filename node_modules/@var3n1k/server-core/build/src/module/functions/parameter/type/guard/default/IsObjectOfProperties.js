import * as GlobalModule from '../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function IsObjectOfProperties(parameter, properties) {
    let InstanceMatch = false;
    if (GlobalModule.Functions.Parameter.Type.Guard.Default.IsObjectWithProperties(parameter, properties)) {
        if (GlobalModule.Functions.Parameter.Type.Manager.Default.Object.GetProperties(parameter).length === properties.length) {
            if (GlobalModule.Functions.Parameter.Type.Manager.Default.Object.GetProperties(parameter).every((_el, _ind, _arr) => properties.includes(_el))) {
                InstanceMatch = true;
            }
        }
    }
    return InstanceMatch;
}
//# sourceMappingURL=IsObjectOfProperties.js.map