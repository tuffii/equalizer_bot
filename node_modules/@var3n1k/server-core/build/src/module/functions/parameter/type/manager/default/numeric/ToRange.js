import * as GlobalModule from '../../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function ToRange(initialNumber, min, max) {
    const Validator = GlobalModule.Classes.Validator;
    Validator.Strict(initialNumber, new Validator().Default.Numeric().Required());
    Validator.Strict(min, new Validator().Default.Numeric());
    Validator.Strict(max, new Validator().Default.Numeric());
    if (Validator.TypeGuard.Default.IsNumeric(min) && Validator.TypeGuard.Default.IsNumeric(max)) {
        if (Validator.TypeGuard.Default.IsBigInt(min)) {
            Validator.Strict(max, new Validator().Default.Numeric().BigInt().Required().Min(min));
        }
        else if (Validator.TypeGuard.Default.IsNumber(min)) {
            Validator.Strict(max, new Validator().Default.Numeric().Number().Required().Min(min));
        }
    }
    if (Validator.TypeGuard.Default.IsNumeric(min) && initialNumber < min)
        initialNumber = min;
    if (Validator.TypeGuard.Default.IsNumeric(max) && initialNumber > max)
        initialNumber = max;
    return initialNumber;
}
//# sourceMappingURL=ToRange.js.map