import * as GlobalModule from '../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function IsEmptyObject(parameter) {
    let InstanceMatch = false;
    if (GlobalModule.Functions.Parameter.Type.Guard.Default.IsObjectOfProperties(parameter, [])) {
        if (GlobalModule.Functions.Parameter.Type.Manager.Default.Object.GetProperties(parameter).length === 0)
            InstanceMatch = true;
    }
    return InstanceMatch;
}
//# sourceMappingURL=IsEmptyObject.js.map