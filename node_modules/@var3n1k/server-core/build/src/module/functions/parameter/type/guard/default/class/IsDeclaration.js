import * as GlobalModule from '../../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function IsDeclaration(parameter) {
    let InstanceMatch = false;
    if (GlobalModule.Functions.Parameter.Type.Guard.Default.IsFunction(parameter)) {
        if (GlobalModule.Functions.Parameter.Type.Guard.Default.IsObjectWithProperties(parameter.prototype, [`constructor`])) {
            if (GlobalModule.Functions.Parameter.Type.Guard.Default.IsFunction(parameter.prototype.constructor)) {
                if (![`Object`, `Function`].includes(parameter.prototype.constructor.name))
                    InstanceMatch = true;
            }
        }
    }
    return InstanceMatch;
}
//# sourceMappingURL=IsDeclaration.js.map