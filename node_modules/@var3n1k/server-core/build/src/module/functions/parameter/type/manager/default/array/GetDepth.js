import * as GlobalModule from '../../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function GetDepth(initialArray) {
    const Validator = GlobalModule.Classes.Validator;
    Validator.Strict(initialArray, new Validator().Default.Array().Required());
    let MaximumArrayDepth = 0;
    const ScanArray = (array, level) => {
        MaximumArrayDepth = level;
        for (let i = 0; i < array.length; i++) {
            const ArrayItem = array[i];
            const IsArrayItemNewSubArray = Validator.TypeGuard.Default.IsArray(ArrayItem);
            if (IsArrayItemNewSubArray) {
                ScanArray(ArrayItem, level + 1);
            }
        }
    };
    ScanArray(initialArray, 1);
    return MaximumArrayDepth;
}
//# sourceMappingURL=GetDepth.js.map