import * as GlobalModule from '../../../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function InList(...elements) {
    const Validator = GlobalModule.Classes.Validator;
    Validator.Strict(elements, new Validator().Default.Array().Required());
    const RandomElementIndex = Math.round(GlobalModule.Functions.Parameter.Type.Manager.Default.Numeric.Random.InRange(0, elements.length - 1));
    const RandomElement = elements[RandomElementIndex];
    return RandomElement;
}
//# sourceMappingURL=InList.js.map