import * as GlobalModule from '../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function IsInstanceOf(parameter, declaration) {
    let InstanceMatch = false;
    if (!GlobalModule.Functions.Parameter.Type.Guard.Default.IsUnknown(parameter)) {
        if (!GlobalModule.Functions.Parameter.Type.Guard.Default.IsUnknown(declaration)) {
            if (parameter instanceof declaration)
                InstanceMatch = true;
        }
    }
    return InstanceMatch;
}
//# sourceMappingURL=IsInstanceOf.js.map