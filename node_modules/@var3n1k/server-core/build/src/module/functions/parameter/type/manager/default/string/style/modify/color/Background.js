import * as GlobalModule from '../../../../../../../../../@module.js';
const BackgroundColorMaskForm = {
    Open: (_) => `\u001B[${48};${2};${_[0]};${_[1]};${_[2]}m`,
    Close: () => `\u001B[${49}m`,
};
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function Background(initialString, ...rgbColor) {
    const Validator = GlobalModule.Classes.Validator;
    Validator.Strict(initialString, new Validator().Default.String().Required());
    Validator.Strict(rgbColor, new Validator().Custom.Color.RGB());
    const ModifiedString = `${BackgroundColorMaskForm.Open(rgbColor)}${initialString}${BackgroundColorMaskForm.Close()}`;
    return ModifiedString;
}
//# sourceMappingURL=Background.js.map