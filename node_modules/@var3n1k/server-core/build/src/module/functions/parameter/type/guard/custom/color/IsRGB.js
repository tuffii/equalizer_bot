import * as GlobalModule from '../../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function IsRGB(parameter) {
    let InstanceMatch = false;
    if (GlobalModule.Functions.Parameter.Type.Guard.Default.IsArray(parameter)) {
        if (parameter.every((_el, _ind, _arr) => GlobalModule.Functions.Parameter.Type.Guard.Default.IsInteger(_el))) {
            if (parameter.every((_el, _ind, _arr) => _el === GlobalModule.Functions.Parameter.Type.Manager.Default.Numeric.ToRange(_el, 0, 255))) {
                if (parameter.length === 3)
                    InstanceMatch = true;
            }
        }
    }
    return InstanceMatch;
}
//# sourceMappingURL=IsRGB.js.map