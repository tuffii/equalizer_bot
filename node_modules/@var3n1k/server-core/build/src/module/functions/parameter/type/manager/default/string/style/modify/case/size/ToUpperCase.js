import * as GlobalModule from '../../../../../../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function ToUpperCase(initialString, symbolIndexes = Array.from(Array(initialString.length), (_el, _ind) => _ind)) {
    const Validator = GlobalModule.Classes.Validator;
    Validator.Strict(initialString, new Validator().Default.String().Required());
    Validator.Strict(symbolIndexes, new Validator().Default.Array()
        .Required()
        .Of(new Validator().Default.Numeric()
        .Number()
        .Required()
        .Integer()
        .Min(0)
        .Max(initialString.length - 1)));
    const ModifiedString = initialString
        .split(/[]{0}/)
        .map((_el, _ind, _arr) => (symbolIndexes.includes(_ind) ? _el.toUpperCase() : _el))
        .join(``);
    return ModifiedString;
}
//# sourceMappingURL=ToUpperCase.js.map