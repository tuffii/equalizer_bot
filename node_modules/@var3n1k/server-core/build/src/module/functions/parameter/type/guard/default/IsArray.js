import * as GlobalModule from '../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function IsArray(parameter) {
    let InstanceMatch = false;
    if (!GlobalModule.Functions.Parameter.Type.Guard.Default.IsUnknown(parameter)) {
        // if (GlobalModule.Functions.Parameter.Type.Guard.Default.IsInstanceOf(parameter, Array<any>))
        if (typeof parameter === 'object') {
            if (Array.isArray(parameter))
                InstanceMatch = true;
        }
    }
    return InstanceMatch;
}
//# sourceMappingURL=IsArray.js.map