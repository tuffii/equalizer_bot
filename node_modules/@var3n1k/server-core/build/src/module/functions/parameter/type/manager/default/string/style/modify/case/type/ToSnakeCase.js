import * as GlobalModule from '../../../../../../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function ToSnakeCase(initialString) {
    const Validator = GlobalModule.Classes.Validator;
    Validator.Strict(initialString, new Validator().Default.String().Required());
    const InitialStringWords = GlobalModule.Functions.Parameter.Type.Manager.Default.String.MultiSplit(initialString, /[\s]+/);
    const InitialStringWordsToUpperCase = InitialStringWords.map((_el, _ind, _arr) => {
        return GlobalModule.Functions.Parameter.Type.Manager.Default.String.Style.Modify.Case.Size.ToUpperCase(_el, Array.from(Array(_el.length), (__el, __ind) => __ind));
    });
    const ModifiedString = InitialStringWordsToUpperCase.join(`_`);
    return ModifiedString;
}
//# sourceMappingURL=ToSnakeCase.js.map