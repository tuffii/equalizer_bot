import * as GlobalModule from '../../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function IsInstance(parameter) {
    let InstanceMatch = false;
    if (GlobalModule.Functions.Parameter.Type.Guard.Default.IsInstanceOf(parameter, Object)) {
        if (typeof parameter === 'object') {
            if (GlobalModule.Functions.Parameter.Type.Guard.Default.IsFunction(parameter.constructor)) {
                if (![`Object`, `Function`].includes(parameter.constructor.name))
                    InstanceMatch = true;
            }
        }
    }
    return InstanceMatch;
}
//# sourceMappingURL=IsInstance.js.map