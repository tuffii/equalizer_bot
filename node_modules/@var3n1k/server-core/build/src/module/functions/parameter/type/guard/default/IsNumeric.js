import * as GlobalModule from '../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function IsNumeric(parameter) {
    let InstanceMatch = false;
    if (!GlobalModule.Functions.Parameter.Type.Guard.Default.IsUnknown(parameter)) {
        if (typeof parameter === 'bigint' || typeof parameter === 'number')
            InstanceMatch = true;
    }
    return InstanceMatch;
}
//# sourceMappingURL=IsNumeric.js.map