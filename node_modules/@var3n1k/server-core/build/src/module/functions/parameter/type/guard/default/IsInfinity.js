import * as GlobalModule from '../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function IsInfinity(parameter) {
    let InstanceMatch = false;
    if (!GlobalModule.Functions.Parameter.Type.Guard.Default.IsNullOrUndefined(parameter)) {
        if (typeof parameter === 'number') {
            if (!Number.isFinite(parameter)) {
                if (parameter === Infinity)
                    InstanceMatch = true;
            }
        }
    }
    return InstanceMatch;
}
//# sourceMappingURL=IsInfinity.js.map