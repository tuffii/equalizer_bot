import * as GlobalModule from '../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function IsObjectWithProperties(parameter, properties) {
    let InstanceMatch = false;
    if (GlobalModule.Functions.Parameter.Type.Guard.Default.IsObject(parameter)) {
        if (GlobalModule.Functions.Parameter.Type.Manager.Default.Object.GetProperties(parameter).length >= properties.length) {
            if (properties.every((_el, _ind, _arr) => GlobalModule.Functions.Parameter.Type.Manager.Default.Object.GetProperties(parameter).includes(_el))) {
                InstanceMatch = true;
            }
        }
    }
    return InstanceMatch;
}
//# sourceMappingURL=IsObjectWithProperties.js.map