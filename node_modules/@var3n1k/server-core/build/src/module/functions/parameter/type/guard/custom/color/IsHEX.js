import * as GlobalModule from '../../../../../../@module.js';
IsHEX.Pattern = {
    HEXColor: {
        RegExp: /^#?(?=[0-9a-fA-F]*$)(?:(.{3}|.{6}))/,
        Group: { Color: { Value: 1 } },
    },
};
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function IsHEX(parameter) {
    let InstanceMatch = false;
    if (GlobalModule.Functions.Parameter.Type.Guard.Default.IsString(parameter)) {
        if (!GlobalModule.Functions.Parameter.Type.Guard.Default.IsEmptyString(parameter)) {
            if (new RegExp(`^${IsHEX.Pattern.HEXColor.RegExp.source}$`, undefined).test(parameter))
                InstanceMatch = true;
        }
    }
    return InstanceMatch;
}
//# sourceMappingURL=IsHEX.js.map