import * as GlobalModule from '../../../../../../@module.js';
//* -------------------------------------------------------------------------
//* Realization
//* -------------------------------------------------------------------------
export default function GetProperties(initialObject) {
    const Validator = GlobalModule.Classes.Validator;
    Validator.Strict(initialObject, new Validator().Default.Object().Required());
    const ObjectSymbolPropertyNames = Object.getOwnPropertySymbols(initialObject);
    const ObjectStringPropertyNames = Object.getOwnPropertyNames(initialObject); // Object.keys(initialObject)
    const ObjectNumberPropertyNames = [];
    const ObjectProperties = [...ObjectSymbolPropertyNames, ...ObjectStringPropertyNames, ...ObjectNumberPropertyNames];
    return ObjectProperties;
}
//# sourceMappingURL=GetProperties.js.map